{{- if eq ( .Values.apiKey | default "" ) "" -}}
{{- fail "apiKey must be provided" -}}
{{- end -}}

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
stringdata:
  apiKey: {{ .Values.apiKey }}
